{
  "name": "orchestrator-extension",
  "private": true,
  "type": "module",
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.80.3",
    "picomatch": "^4.0.4"
  },
  "scripts": {
    "test": "npx vitest run",
    "typecheck": "npx tsc --noEmit"
  }
}
