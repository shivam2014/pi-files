# Orchestrator Refactor Audit

Audit compiled from recent orchestrator-extension sessions. Each row maps a symptom to its root cause, current status, proposed/applied fix, affected files, cache/token impact, and priority.

## Legend

- ✅ Fixed
- 🛠️ Partially fixed
- ⏳ Open / not started
- ❌ Won't fix

## Areas

### 1. Fusion

| ID | Symptom | Root cause | Status | Proposed fix or fix applied | Files affected | Cache / token impact | Priority |
|---|---|---|---|---|---|---|---|
| FUS-001 | Panel model returns empty response and is silently dropped from fusion result | `runPanelModel` returned `{ error: "Empty response" }` without surfacing why; downstream `succeeded` filter discarded the model | ✅ Fixed | Added explicit empty-response detection with debug logging; if collected text exists, return it as content even when no tool calls; fallback pushes text as a report when no `reportFinding` calls were made | `/Users/shivam94/pi-files/extensions/orchestrator/fusion-tool.ts` | Low: no system-prompt/tool-schema change; only runtime handling of response content | P1 |
| FUS-002 | Judge fails to parse JSON analysis, falling back to raw panel responses | `parseJudgeAnalysis` requires all schema fields; judge sometimes returns partial JSON or prose; `extractJsonObject` is brittle with nested fences | 🛠️ Partially fixed | Added 3-attempt retry loop with parse-error feedback; still uses hand-rolled JSON extractor instead of SDK parser; schema validation should be relaxed or use Zod/typebox runtime schema | `/Users/shivam94/pi-files/extensions/orchestrator/fusion-tool.ts` | Medium: judge prompt/schema tweaks can invalidate cached prefix for judge model; keep system prompt stable and only change user feedback | P1 |
| FUS-003 | Panel models run sequentially or with unbounded concurrency | `mapWithConcurrencyLimit` caps at 2, but no backoff or queue health checks; slow models block the whole fusion call | ✅ Fixed | Concurrency limit of 2 with per-model 30 s timeout; consider adaptive concurrency based on provider rate limits | `/Users/shivam94/pi-files/extensions/orchestrator/fusion-tool.ts` | Low: runtime concurrency only; no cache/schema impact | P2 |
| FUS-004 | Live progress only reports at model completion, not during streaming | `runPanelModel` aggregates reports and emits one `onUpdate` per model; no streaming delta handling | ⏳ Open | Stream panel-model deltas via `complete` streaming and emit `panel_reports` updates incrementally; cap update frequency to avoid TUI thrash | `/Users/shivam94/pi-files/extensions/orchestrator/fusion-tool.ts`, `peek-overlay.ts` | Medium: adding streaming may change tool-result shape; ensure resumed sessions see compatible details | P2 |
| FUS-005 | Model resolution fails silently and falls back to auto-diverse panel without notifying user | `resolveModels` returns `null` for bad IDs; `resolveOneModel` only handles `provider/id` format; no warning is surfaced | 🛠️ Partially fixed | Auto-diverse fallback picks from available providers; should also emit a notification when configured model is invalid and log explicit resolution errors | `/Users/shivam94/pi-files/extensions/orchestrator/fusion-tool.ts` | Low: config read does not change system prompt; only user notification | P2 |
| FUS-006 | Stale or missing `fusion.json` config causes inconsistent panel behavior | `loadFusionConfig` merges global + project configs once per `execute`; edits via `/fusion` write file but in-memory parent config may still be stale | 🛠️ Partially fixed | `saveFusionConfig` reloads and merges; still no file watcher or cache invalidation; recommend reloading config on every `execute` and timestamping | `/Users/shivam94/pi-files/extensions/orchestrator/fusion-tool.ts`, `fusion-commands.ts`, `fusion-tui.ts` | Medium: `fusion.json` is state on disk; tool schema is stable, but result contents vary by config | P2 |
| FUS-007 | Output mixes panel reports and judge synthesis inconsistently | `formatFusionResult` renders both full panel reports and a judge section; duplicated consensus items appear in both Panel and Judge sections | ⏳ Open | Restructure output: Panel section shows only per-model bullets; Judge section shows synthesis only; remove duplicated consensus from judge view or label as "confirmed" | `/Users/shivam94/pi-files/extensions/orchestrator/fusion-tool.ts` | Low: output formatting only | P2 |
| FUS-008 | `extractText` only prefers text blocks then thinking blocks; ignores other content | For models that return reasoning under a different type, extraction falls back to empty and triggers FUS-001 | 🛠️ Partially fixed | Current fallback to `thinking` covers common cases; should use SDK content normalization helper or iterate all known content types | `/Users/shivam94/pi-files/extensions/orchestrator/fusion-tool.ts` | Low | P3 |
| FUS-009 | No upper bound on `reportFinding` loops | A model can loop tool calls up to `maxLoops` (10); malformed models may never produce final text | ✅ Fixed | `maxLoops` guard returns accumulated text or error after 10 iterations | `/Users/shivam94/pi-files/extensions/orchestrator/fusion-tool.ts` | Low | P2 |
| FUS-010 | Fusion tool schema lacks optional `specialist` or `models` override | Users cannot request a specific model for a one-off fusion call | ⏳ Open | Add optional `panel`/`judge` string array parameters to the tool schema, validated against registry | `/Users/shivam94/pi-files/extensions/orchestrator/fusion-tool.ts` | Medium: tool schema change breaks resumed sessions; only do when breaking changes are acceptable | P3 |

### 2. Plan Panel (Layer 1)

| ID | Symptom | Root cause | Status | Proposed fix or fix applied | Files affected | Cache / token impact | Priority |
|---|---|---|---|---|---|---|---|
| PAN-001 | Unrelated steps from a previous session appear under the current plan | `setupPlanPanel` preserves `oldSteps` whenever `goal` matches; identical goal strings across unrelated tasks merge histories | 🛠️ Partially fixed | `sameGoal` merge was intended for hot-reload; should scope by `_sessionId` and never merge across user-initiated sessions unless explicitly resuming | `/Users/shivam94/pi-files/extensions/orchestrator/plan-panel.ts` | Low: UI state only; no cache impact | P1 |
| PAN-002 | Pending substeps are not visible in the plan panel | `toFeedState` only emits completed reports, done tool calls, and the active spinner line; pending substeps are filtered out | ⏳ Open | Include pending substeps (capped) in `toFeedState` so the widget shows upcoming work | `/Users/shivam94/pi-files/extensions/orchestrator/plan-panel.ts` | Low: widget rendering only | P2 |
| PAN-003 | Errored steps render as active/spinning instead of failed | `toFeedState` maps `ps.active` to spinner and does not check `ps.errored`; `renderActivityFeed` error branch handles ✗ but plan panel conversion loses it | ⏳ Open | Pass `errored` flag through `PlanStep` to `toFeedState` and render ✗ in the widget | `/Users/shivam94/pi-files/extensions/orchestrator/plan-panel.ts` | Low | P1 |
| PAN-004 | Initial plan shows all steps pending but none active | `setupPlanPanel` sets `active: !wasCompleted && i === 0`, so only the first uncompleted step is active; this is intentional but can look like no progress on first call | ✅ Fixed (by design) | First pending step is active; remaining steps are pending dots; no code change required | `/Users/shivam94/pi-files/extensions/orchestrator/plan-panel.ts` | None | P3 |
| PAN-005 | `trimToBudget` can drop the goal line or progress dots | The function claims to always keep goal/dots but the fallback path (`activeIdx < 0`) simply returns the last `budget` lines, which may skip them | ⏳ Open | Refactor `trimToBudget` to guarantee indices 0 and 1 are kept, then fill remaining budget from the bottom up | `/Users/shivam94/pi-files/extensions/orchestrator/plan-panel.ts` | Low | P2 |
| PAN-006 | No retry indicator in the plan panel | `retryPlanStep` exists but is not wired to delegation retries; only the activity feed shows retry counts | ⏳ Open | Call `retryPlanStep` when the orchestrator retries a failed delegation, or mirror feed retry state in plan panel | `/Users/shivam94/pi-files/extensions/orchestrator/plan-panel.ts`, `delegate-tool.ts` | Low | P2 |
| PAN-007 | Plan panel state persists across `clearPlanPanel` via stale timer closures | Global timer registry helps, but `_setWidget` and `_lastWidgetContent` are module-level and can reference a destroyed context | 🛠️ Partially fixed | Timer self-check kills stale intervals; still need to nullify `_setWidget` and `_lastWidgetContent` on every `clearPlanPanel` | `/Users/shivam94/pi-files/extensions/orchestrator/plan-panel.ts` | Low | P2 |
| PAN-008 | `BUDGET = 9` hard cap may clip multi-step plans with many substeps | Fixed budget does not adapt to terminal height | ⏳ Open | Derive budget from `ctx.ui.height` or TUI layout, with a minimum of 6 | `/Users/shivam94/pi-files/extensions/orchestrator/plan-panel.ts` | Low | P3 |
| PAN-009 | Saved plan state loses `active` flag and `sessionId` | `savePlanState` serializes only `label/completed/errored/startTime`; reload reconstructs `active: false` and uses stale session id logic | ⏳ Open | Persist full `PlanStep` state and session id; validate session id on load | `/Users/shivam94/pi-files/extensions/orchestrator/plan-panel.ts` | Low | P3 |
| PAN-010 | `finalizePlanStep` duplicates logic with `completePlanStep` | Two functions both set completed/active/endTime and cap substep lines; divergence risk | ⏳ Open | Consolidate into a single `closePlanStep(status)` helper that accepts `completed`/`errored`/`aborted` | `/Users/shivam94/pi-files/extensions/orchestrator/plan-panel.ts` | Low | P3 |

### 3. Activity Feed (Layer 2)

| ID | Symptom | Root cause | Status | Proposed fix or fix applied | Files affected | Cache / token impact | Priority |
|---|---|---|---|---|---|---|---|
| FEED-001 | Active step + pending substeps disappear in certain render paths | In the error branch, pending substeps after the active one are intentionally dropped (`// else: pending substeps after active one — not shown`); in `toFeedState` only active/completed substeps are emitted | ⏳ Open | Render pending substeps in error path and plan-panel conversion with a cap, preserving intent while avoiding silent drops | `/Users/shivam94/pi-files/extensions/orchestrator/activity-feed.ts`, `plan-panel.ts` | Low | P2 |
| FEED-002 | `MAX_FEED_SUBSTEPS = 8` silently drops older substeps | `addSubstep` slices `step.substeps.slice(1)` when over limit without any history summary | ⏳ Open | Keep a collapsed "+N more" indicator and/or write full trail to `details` so it is still available to the parent agent | `/Users/shivam94/pi-files/extensions/orchestrator/activity-feed.ts` | Low | P2 |
| FEED-003 | `addStep` heuristic merges unrelated steps | It replaces an existing step if the new label starts with the old label, the old has no substeps, and is not completed; common prefixes cause false merges | ⏳ Open | Require exact label match for merge, or track step identity with stable IDs from `planSteps` | `/Users/shivam94/pi-files/extensions/orchestrator/activity-feed.ts` | Low | P2 |
| FEED-004 | Error rendering only shows one active substep and hides the rest | `markFeedError` keeps the first pending substep as "active" and discards later pending ones in the render loop | 🛠️ Partially fixed | Error render intentionally focused; still need a "+N pending" hint to avoid misleading "only one substep" impression | `/Users/shivam94/pi-files/extensions/orchestrator/activity-feed.ts` | Low | P2 |
| FEED-005 | `completeLastSubstep` and `setToolDetail` duplicate "find first uncompleted substep" logic | Four functions replicate the same loop; bugs in one propagate | ⏳ Open | Extract `findActiveSubstepIndex()` helper and reuse | `/Users/shivam94/pi-files/extensions/orchestrator/activity-feed.ts` | Low | P3 |
| FEED-006 | `toolCallToSubstep` normalizes paths by taking the last segment, losing directory context | `normalizePath` returns basename for long paths; multiple files with same basename collide | ⏳ Open | Preserve relative path up to a max width, or include parent dir when truncated | `/Users/shivam94/pi-files/extensions/orchestrator/activity-feed.ts` | Low | P3 |
| FEED-007 | `renderActivityFeed` re-renders recursively when `currentStep` changes mid-render | `MAX_RENDER_RETRIES` guard helps but can still produce inconsistent snapshots | 🛠️ Partially fixed | Snapshot `_currentStep` at start; consider making render pure and queueing updates outside render | `/Users/shivam94/pi-files/extensions/orchestrator/activity-feed.ts` | Low | P3 |
| FEED-008 | No output-preview truncation standard | Each preview is sliced to 80 chars ad-hoc; error output uses a different path | ⏳ Open | Use a single `truncatePreview(text, max)` helper across `completeLastSubstep`, `tool_execution_end`, and `updateActiveSubstepOutput` | `/Users/shivam94/pi-files/extensions/orchestrator/activity-feed.ts`, `subagent-runner.ts` | Low | P3 |

### 4. Subagent Runner

| ID | Symptom | Root cause | Status | Proposed fix or fix applied | Files affected | Cache / token impact | Priority |
|---|---|---|---|---|---|---|---|
| RUN-001 | Module-level globals (`_batchLoadSubagent`, `_planParsed`, `OUTPUT_CAP`) are shared across concurrent subagents | Variables defined at module scope; multiple `runSubagent` calls can race on these flags | ⏳ Open | Move per-session state into a `SubagentSession` object passed through the lifecycle | `/Users/shivam94/pi-files/extensions/orchestrator/subagent-runner.ts` | Low: internal state only; no cache impact | P1 |
| RUN-002 | `SUBAGENT_ENV_KEY` leaks to child processes and can contaminate non-subagent contexts | `process.env[SUBAGENT_ENV_KEY] = "1"` is set before loader and deleted in `finally`; a throw before cleanup or a spawned child inherits it | 🛠️ Partially fixed | Cleanup in `finally` exists; use `process.env` only during `loader.reload()` and unset immediately after; avoid process-wide env for session marking | `/Users/shivam94/pi-files/extensions/orchestrator/subagent-runner.ts`, `index.ts` | Medium: env changes can affect tool child-process behavior; minimize window | P1 |
| RUN-003 | `turn_end` handler is empty; turn-level summaries not captured | `if (event.type === "turn_end") { const results = ...; }` does nothing with `results` | ⏳ Open | Build per-turn summary from `toolResults` and append to output or feed; emit `onUpdate` with turn summary | `/Users/shivam94/pi-files/extensions/orchestrator/subagent-runner.ts` | Low | P2 |
| RUN-004 | Subagent output capped by manual 30 000-char slice | `OUTPUT_CAP` is hard-coded and slices without word/line awareness; truncates mid-sentence | ⏳ Open | Use SDK truncation helper (e.g., `truncateToBudget`) or a semantic truncation that preserves headers/findings/audit | `/Users/shivam94/pi-files/extensions/orchestrator/subagent-runner.ts` | High: changing output size changes context consumed by parent; ensure cache prefix after truncation stays stable | P1 |
| RUN-005 | `message_update` updates plan panel with `feed.goal` instead of substep lines | `updatePlanStepDetail(feed.goal || "")` ignores actual tool progress; plan panel stays static during streaming | ⏳ Open | Pass `renderSubstepLines(activeStep.substeps)` or tool-detail preview to `updatePlanStepDetail` | `/Users/shivam94/pi-files/extensions/orchestrator/subagent-runner.ts` | Low | P1 |
| RUN-006 | `planSteps`/`advanceStep`/`reportFinding` tools can be called before `planSteps` in the subagent | The parent `index.ts` guard only works while `_batchLoadSubagent > 0`, which is decremented after loader initialization; the actual subagent session has no tool_call guard | 🛠️ Partially fixed | Mandate via system prompt (`STEPS_MANDATE`); add tool-level guard inside `execute` of each tool to block out-of-order calls | `/Users/shivam94/pi-files/extensions/orchestrator/subagent-runner.ts`, `specialists.ts` | Medium: adding tool-level validation changes tool behavior; safe if messages are clear | P1 |
| RUN-007 | `customTools` array and `SUBAGENT_ENV_KEY` duplicate the same isolation intent | Two mechanisms attempt to prevent orchestrator re-registration; mismatch can cause one to work and the other to fail | ⏳ Open | Standardize on one mechanism: either pass session context through `customTools` metadata or use the env flag, not both | `/Users/shivam94/pi-files/extensions/orchestrator/subagent-runner.ts`, `index.ts` | Medium: changing registration guard affects extension loading and cacheability | P2 |
| RUN-008 | `runSubagent` catch block returns different result shape than success path | Error path returns `{ output, turns }` without `elapsed_ms` or `toolCallTrail` | ⏳ Open | Normalize return shape so `delegate-tool.ts` can always read `toolCallTrail` and `elapsed_ms` | `/Users/shivam94/pi-files/extensions/orchestrator/subagent-runner.ts` | Low | P2 |
| RUN-009 | `loader` initialization runs inside `try/finally` but `_planParsed` reset occurs there too | Resetting `_planParsed` before the session starts means the parent guard may be cleared too early | 🛠️ Partially fixed | Reset per-session state in the session object's teardown, not module globals | `/Users/shivam94/pi-files/extensions/orchestrator/subagent-runner.ts` | Low | P2 |
| RUN-010 | `turns` counter only increments on `message_end` for assistant role | Tool-only turns are not counted; metrics are inaccurate | ⏳ Open | Count every completed model turn (assistant text or tool result round-trip) | `/Users/shivam94/pi-files/extensions/orchestrator/subagent-runner.ts` | Low | P3 |
| RUN-011 | Scope file is written per subagent but no enforcement file exists | `writeScopeFile` creates `.pi/scope.json`; there is no `scope-guard.ts` in the extension to actually enforce it | ✅ Fixed | `scope-guard.ts` exists — reads `.pi/scope.json`, enforces path/line limits, emits ScopeExpansionRequest, fail-closed on stale/malformed | `/Users/shivam94/pi-files/extensions/orchestrator/scope-guard.ts` | Medium: adding a new guard extension changes tool behavior and may alter cached tool results | P1 |
| RUN-012 | Bash excluded for writer/researcher only; other specialists can still run arbitrary bash | `excludeTools` removes `bash` for two specialists, but coder and others retain it without further runtime constraints | ⏳ Open | Keep bash for coder but add command allow-list/deny-list in subagent harness | `/Users/shivam94/pi-files/extensions/orchestrator/subagent-runner.ts`, `specialists.ts` | Medium: tool availability changes context shape | P2 |

### 5. Lint Guard

| ID | Symptom | Root cause | Status | Proposed fix or fix applied | Files affected | Cache / token impact | Priority |
|---|---|---|---|---|---|---|---|
| LINT-001 | Fake `role:tool` lint messages are coerced to custom `sendMessage` instead of real tool results | `emitLintResult` uses `pi.sendMessage({ customType: "lint", ... })` because the comment says it must not modify tool_result content | 🛠️ Partially fixed | This is cache-safe but creates synthetic messages; consider emitting as a real `tool_result` with `customType` when SDK supports it, or clearly prefix so orchestrator can filter | `/Users/shivam94/pi-files/extensions/lint-guard.ts` | Medium: message role/format changes affect conversation structure and caching | P2 |
| LINT-002 | Project-agnostic path resolution can pick wrong config in monorepos | `walkUpForConfig` walks from the edited file, but standalone checks run in `dirname(resolvedFile)`; may miss workspace root tools | ⏳ Open | Accept optional project root from `ctx.cwd`, prefer nearest config, and run project-level commands from the config directory consistently | `/Users/shivam94/pi-files/extensions/lint-guard.ts` | Low: only command cwd changes; no prompt/schema impact | P2 |
| LINT-003 | `runTool` timeout is hard-coded at 10 s | Large projects can exceed this; no progress update | ⏳ Open | Make timeout configurable and stream partial output via `onUpdate` | `/Users/shivam94/pi-files/extensions/lint-guard.ts` | Low | P3 |
| LINT-004 | Auto-lint does not lint generated files until after edit/write; no pre-check | Catches errors only after mutation | ⏳ Open | Optional pre-edit typecheck and batch lint after a delegation step completes | `/Users/shivam94/pi-files/extensions/lint-guard.ts` | Low | P3 |

### 6. Token Saver / Nyro-Sync / Commands

| ID | Symptom | Root cause | Status | Proposed fix or fix applied | Files affected | Cache / token impact | Priority |
|---|---|---|---|---|---|---|---|
| TOK-001 | `/caveman` command accepts invalid input with info-level notification | Validation branch only notifies `"Usage: ..."` with severity `"info"`; typos are not warnings | ⏳ Open | Change invalid-arg notification to `"warning"`; add `/caveman status` subcommand | `/Users/shivam94/pi-files/extensions/token-saver.ts` | Low | P3 |
| TOK-002 | Notify severity is always `"info"` regardless of mode change severity | All `ctx.ui.notify` calls hard-code `"info"` | ⏳ Open | Use `"warning"` for disabling token saver or ultra mode, `"info"` otherwise | `/Users/shivam94/pi-files/extensions/token-saver.ts` | Low | P3 |
| TOK-003 | Caveman prompt injection changes the system prompt and invalidates provider cache | `before_agent_start` appends `TERSE_PROMPTS[currentTerseMode]` to every system prompt | ✅ Fixed (by design) | This is the intended trade-off; document that mode switches clear the cache; consider making prompt append stable with a marker so provider cache can match on prefix | `/Users/shivam94/pi-files/extensions/token-saver.ts` | High: any system prompt change invalidates provider cache; stable prefix reordering is safe only if structural markers preserved | P2 |
| TOK-004 | Read dedup fingerprint key uses raw path, missing normalization | `path` may be relative or absolute; same file gets different fingerprints | ⏳ Open | Normalize path with `resolve(ctx.cwd, path)` before hashing/storing | `/Users/shivam94/pi-files/extensions/token-saver.ts` | Low | P3 |
| TOK-005 | `nyro-sync.ts` extension does not exist in the repository | No file found at expected paths | ⏳ Open | Create `nyro-sync.ts` with typed sync helpers, or remove references from orchestrator docs | `/Users/shivam94/pi-files/extensions/nyro-sync.ts` (missing) | N/A | P3 |
| CMD-001 | `timeline-diff` command used `.length` on diff object instead of `.count` | Earlier code accessed `diff.length` which is undefined; current code uses `diff.count` | ✅ Fixed | `commands.ts` now reads `diff.count` from `getTimelineDiff()` | `/Users/shivam94/pi-files/extensions/orchestrator/commands.ts` | Low | P2 |
| CMD-002 | `/debug-orchestrator` status branch does not distinguish "status" from empty args | Calling with no args runs snapshot; "status" is not a recognized keyword | ⏳ Open | Treat `"status"` and empty args the same, or add a dedicated status line with debug on/off and feed summary | `/Users/shivam94/pi-files/extensions/orchestrator/commands.ts` | Low | P3 |
| CMD-003 | `/debug-orchestrator` snapshot writes to `/tmp` without confirming success | Write errors are logged to stderr but user still sees success message | ⏳ Open | Return error notification if snapshot write fails | `/Users/shivam94/pi-files/extensions/orchestrator/commands.ts` | Low | P3 |
| CMD-004 | `/render` and `/timeline` dump to `/tmp` unconditionally | Hard-coded paths may fail in restricted environments | ⏳ Open | Allow override via env var or write to `.pi/` logs instead | `/Users/shivam94/pi-files/extensions/orchestrator/commands.ts` | Low | P3 |

### 7. Harness / Orchestration Workflow

| ID | Symptom | Root cause | Status | Proposed fix or fix applied | Files affected | Cache / token impact | Priority |
|---|---|---|---|---|---|---|---|
| HAR-001 | Command-exit/bash noise appears in subagent UI | Subagent tool events do not filter non-informative shell exit/status lines; they are added as substeps | ⏳ Open | In `toolCallToSubstep`, suppress noisy labels (e.g., `bash: exit 0`, empty commands) or give them a collapsed icon | `/Users/shivam94/pi-files/extensions/orchestrator/activity-feed.ts`, `subagent-runner.ts` | Low | P2 |
| HAR-002 | Duplicate delegate render (call text + result text) | `delegate` tool registers both `renderCall` and `renderResult`, causing two visual components unless TUI collapses them | 🛠️ Partially fixed | `renderCall` shows title, `renderResult` shows final feed; verify TUI deduplicates partial and final renders; consider suppressing `renderCall` text if result will replace it | `/Users/shivam94/pi-files/extensions/orchestrator/delegate-tool.ts` | Low | P2 |
| HAR-003 | Scope guard escalates instead of terminating on violation | `writeScopeFile` writes scope; no guard terminates the subagent; deviations are only reported in `[Audit: ...]` after completion | ✅ Fixed | `scope-guard.ts` blocks `edit`/`write` outside scope and returns error tool result | `/Users/shivam94/pi-files/extensions/orchestrator/scope-guard.ts` | High: adding a blocking guard changes tool outcomes and may alter cached tool results; design as cache-neutral validation layer | P1 |
| HAR-004 | Delegation metrics are reset per subagent but not surfaced to parent plan | `metrics` object in `delegate-tool.ts` is local; scope violations are appended to output but not to plan state | ⏳ Open | Return metrics in tool `details` and optionally render a compact metrics row in the plan panel | `/Users/shivam94/pi-files/extensions/orchestrator/delegate-tool.ts`, `plan-panel.ts` | Low | P3 |
| HAR-005 | Orchestrator system prompt is appended every time `before_agent_start` fires | Guard checks for marker `## Orchestrator Mode` and returns early, but if the prompt is regenerated without the marker it would double | ✅ Fixed | Marker-based deduplication prevents duplicate injection | `/Users/shivam94/pi-files/extensions/orchestrator/index.ts` | Medium: prompt duplication would bloat context; current guard is safe | P2 |
| HAR-006 | `before_agent_start` registers fusion tool conditionally; schema not stable across sessions | `registerFusionTool(pi, ctx.cwd)` is called inside the handler; if config is disabled the tool may not appear, breaking resumed sessions that expected it | 🛠️ Partially fixed | Tool is registered conditionally; recommend always registering `fusion` and returning disabled status when config disables it | `/Users/shivam94/pi-files/extensions/orchestrator/index.ts`, `fusion-tool.ts` | High: tool schema changes break resumed sessions | P1 |
| HAR-007 | `tool_call` handler blocks non-delegate calls but does not validate delegate parameters | No input sanitization on `specialist`, `task`, or `scope` before `runSubagent` | ⏳ Open | Add lightweight validation in the handler (e.g., max task length, known specialist) before executing | `/Users/shivam94/pi-files/extensions/orchestrator/index.ts`, `delegate-tool.ts` | Low | P2 |

### 8. Architecture / SDK Alignment

| ID | Symptom | Root cause | Status | Proposed fix or fix applied | Files affected | Cache / token impact | Priority |
|---|---|---|---|---|---|---|---|
| ARCH-001 | Tool schema slot-filling missing: `plan` tool has `steps[]` but no step IDs; `delegate` has no `stepId` | Current schemas use simple string arrays; orchestrator cannot correlate delegations with planned steps except by label | ⏳ Open | Add optional `stepId` (string/number) to `plan` step objects and `delegate` parameters; update `startDelegationStep` to match by ID | `/Users/shivam94/pi-files/extensions/orchestrator/plan-tool.ts`, `delegate-tool.ts`, `types.ts` | High: tool schema changes break resumed sessions; batch with other schema changes | P1 |
| ARCH-002 | Tool-level guard rails are minimal | `index.ts` blocks non-delegate calls but does not guard argument shape, budget, or scope preconditions | ⏳ Open | Introduce per-tool middleware that validates args, enforces max lengths, and rejects coder without scope | `/Users/shivam94/pi-files/extensions/orchestrator/index.ts`, `delegate-tool.ts` | Medium: adding validation changes error responses; keep messages deterministic | P2 |
| ARCH-003 | Specialist prompts are prose-heavy; no numbered decision tree for errors/retries | Subagents are told to report findings and audit but not given a clear branching decision tree | ⏳ Open | Add a short "If X then Y" numbered decision tree to `STEPS_MANDATE` and each specialist prompt | `/Users/shivam94/pi-files/extensions/orchestrator/specialists.ts` | Medium: prompt text changes invalidate provider cache; prepend stable markers and append new sections after them | P2 |
| ARCH-004 | Retry reasons are plain strings, not enums | `retryFeedStep` and retry logic accept `reason?: string`; analytics and UI cannot categorize retries | ⏳ Open | Define a `RetryReason` enum/type union (e.g., `error`, `timeout`, `scope_deviation`, `user_request`) and propagate through feed/plan | `/Users/shivam94/pi-files/extensions/orchestrator/activity-feed.ts`, `plan-panel.ts`, `types.ts` | Low | P2 |
| ARCH-005 | State lives in module globals instead of tool-result details | `planState`, `_activeDelegations`, `_cachedScope`, feed state, etc. are module-level; resumption or parallel runs are fragile | ⏳ Open | Carry session state in a serializable object stored in `ctx` / tool `details` / `.pi/` files; pass state objects explicitly | `/Users/shivam94/pi-files/extensions/orchestrator/plan-panel.ts`, `activity-feed.ts`, `delegate-tool.ts`, `subagent-runner.ts` | High: moving state changes how sessions resume; keep backward-compatible fallback for old sessions | P1 |
| ARCH-006 | `customTools` and `SUBAGENT_ENV_KEY` duplicate isolation intent | Two mechanisms increase complexity and risk of inconsistent state | ⏳ Open | Pick one: pass subagent flag through SDK session options or use env; document the choice | `/Users/shivam94/pi-files/extensions/orchestrator/subagent-runner.ts`, `index.ts` | Medium | P2 |
| ARCH-007 | Output truncation does not use SDK helpers | Manual slice in `subagent-runner.ts` and `compressOutput`; no semantic awareness | ⏳ Open | Adopt SDK truncation/compression utilities when available; preserve structured sections (Findings, Audit) | `/Users/shivam94/pi-files/extensions/orchestrator/subagent-runner.ts`, `activity-feed.ts` | High: truncation affects context size and cache prefix | P1 |
| ARCH-008 | Type safety gaps across orchestrator modules | Several `any` types (e.g., `model: any`, `registry: any`, event payloads cast with `as any`) | ⏳ Open | Add shared types for model registry, SDK events, and tool results; replace `any` with narrow types | `/Users/shivam94/pi-files/extensions/orchestrator/*.ts` | Low: type-only changes do not affect runtime/cache | P3 |
| ARCH-009 | No formal schema versioning for orchestrator tool results | `details` objects vary by code version; old sessions may deserialize unexpected shapes | ⏳ Open | Add `version` field to tool-result `details` and write migration helpers | `/Users/shivam94/pi-files/extensions/orchestrator/types.ts`, `plan-tool.ts`, `delegate-tool.ts`, `fusion-tool.ts` | Medium: version field is additive and safe | P3 |
| ARCH-010 | `specialists.ts` system prompts are concatenated with template literals; hard to localize/test | Prompt construction mixes instructions, examples, and output format in one string | ⏳ Open | Split prompts into composable sections (persona, workflow, output format, caveman) and unit-test each | `/Users/shivam94/pi-files/extensions/orchestrator/specialists.ts` | Medium: prompt reordering safe if structural markers preserved; avoid reordering existing stable prefix | P3 |
| ARCH-011 | `index.ts` registers fusion command and tool unconditionally even when not needed | `registerFusionCommands` always added; `registerFusionTool` added in handler | 🛠️ Partially fixed | `registerFusionTool` is conditional; recommend making both unconditional and using config to short-circuit execution | `/Users/shivam94/pi-files/extensions/orchestrator/index.ts`, `fusion-tool.ts` | High: unconditional registration stabilizes schema but increases loaded surface | P2 |

## Execution Order

Recommended order so each layer stabilizes before the next:

1. **State hygiene** — RUN-001, RUN-002, ARCH-005, ARCH-006 (move globals to session objects, isolate env flag).
2. **Schema stability** — ARCH-001, ARCH-002, HAR-006, ARCH-011 (fix tool schemas, then keep them stable).
3. **Output & truncation** — RUN-004, ARCH-007, FEED-002, FEED-008 (use SDK helpers, preserve structure).
4. **Scope & guard rails** — ~~HAR-003~~, ~~RUN-011~~, ARCH-002, RUN-012 (scope-guard done; add per-tool validation).
5. **Plan panel accuracy** — PAN-003, PAN-002, PAN-005, PAN-006, PAN-001 (error/pending/retry rendering and session scoping).
6. **Activity feed correctness** — FEED-003, FEED-001, FEED-004, FEED-005 (merge logic, pending substeps, helpers).
7. **Fusion robustness** — FUS-002, FUS-005, FUS-007, FUS-004 (judge parsing, model resolution, output format, streaming).
8. **Lint/token/commands polish** — LINT-002, TOK-001, TOK-002, CMD-002, CMD-003 (lower priority surface fixes).
9. **Prompt architecture** — ARCH-003, ARCH-010, TOK-003 docs (decision trees, testable prompts, cache trade-offs).
10. **Type safety & versioning** — ARCH-008, ARCH-009 (pay-down tech debt without user-facing changes).
